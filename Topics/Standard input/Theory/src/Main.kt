import java.util.*

// You can experiment here, it won’t be checked

fun main(args: Array<String>) {

    val (a, b) = readLine()!!.split(' ')

    println(a) // prints the second number
    println(b) // prints the first number
}
